import { css } from "linaria";

export const styles = {
  wrapper: css`
    background: #3f3764;
    border-radius: var(--radius);
    font-size: var(--font-size-s);
    padding: 3px 5px;
    white-space: nowrap;
  `,
};
